/* exported last */
const bestBand = ['John', 'Paul', 'George', 'Ringo'];

// console.log(bestBand[bestBand.length - 1]);

function last(array) {
  const hershey = array.length - 1;
  return array[hershey];
}

console.log(last(bestBand));
